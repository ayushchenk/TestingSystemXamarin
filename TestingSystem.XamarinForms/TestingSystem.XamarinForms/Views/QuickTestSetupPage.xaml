<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:b="clr-namespace:TestingSystem.XamarinForms.Behaviors"
             x:Class="TestingSystem.XamarinForms.Views.QuickTestSetupPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Specialization"/>
            <Picker ItemsSource="{Binding Specializations}" SelectedItem="{Binding SelectedSpecialization}"/>
            <Label Text="Subject"/>
            <Picker ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}"/>
            <Label Text="Question count"/>
            <Entry Keyboard="Numeric" Text="{Binding QuestionCount}">
                <Entry.Behaviors>
                    <b:NumericValidationBehavior/>
                </Entry.Behaviors>
            </Entry>
            <Button Command="{Binding NextCommand}" Text="Next"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>